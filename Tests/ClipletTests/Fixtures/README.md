These synthetic, frozen documents represent previously supported wire formats.
Do not regenerate them with the current model's encoder. Add a new fixture when
introducing a schema version; retain earlier fixtures unchanged. The v1 attachment
is the SHA-256 reference for empty bytes and tests storage compatibility, not image decoding.
